# First 10 Fixes

1. Link this pack from a root/current-status doc.
2. Add explicit `DEPRECATED.md` inside `evida-core/backend-api` or move it to legacy.
3. Add one command that runs boundary verification + desktop tests + build.
4. Add a clean-machine release smoke checklist result file.
5. Add UI smoke test coverage for Saksrom import and source opening.
6. Add security limitation copy near release notes and onboarding.
7. Add dependency scan/SBOM TODO to release checklist.
8. Confirm Tauri CSP and allowed connect sources are intentional.
9. Create Spring Boot verification note: how and where `mvn test` must pass.
10. Create stale-doc inventory under `docs/`.
